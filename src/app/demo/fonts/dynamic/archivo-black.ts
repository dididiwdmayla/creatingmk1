import { Archivo_Black } from "next/font/google";

const archivoBlack = Archivo_Black({
  variable: "--font-demo-archivo-black",
  subsets: ["latin"],
  weight: "400",
  preload: false,
});

export default archivoBlack;
